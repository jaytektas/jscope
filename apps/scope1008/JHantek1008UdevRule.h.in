#pragma once

// GENERATED by CMake from packaging/60-hantek-1008c.rules -- edit that file, never this one. jscope
// carries the rule so a copy with no install step (an AppImage) can install it itself (JUdevRule).

inline constexpr const char* kHantek1008UdevRule = R"UDEV(@JSCOPE_UDEV_RULE@)UDEV";
